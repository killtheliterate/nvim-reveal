'lil convenience... opens the buffer using `xdg-open`

```lua
  {
    "killtheliterate/nvim-reveal",
    config = function()
      require('nvim-reveal').setup {
        trigger_key = "<leader>R" -- or, you know...
      }
    end
  },
```
